extends RefCounted
class_name Prefabs

# when adding new - use paths, if godot has not generated a UID yet.
# do not use \ switches

#this is the default file for prefabs. if we have enough (like blocks or menus) - we can create a new file to hold them
